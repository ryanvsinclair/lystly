import { StudioThemes } from "./StudioThemes";

export const metadata = {
  title: "Studio buttons · Lystly",
  robots: { index: false, follow: false },
};

export default function StudioTempPage() {
  return <StudioThemes />;
}
